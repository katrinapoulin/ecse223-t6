class Student {
	id;
	name;
}
class Course {
	description;
	code;
}

class CourseSection {
	Date term;
	sectionLetter;
	1..* -- 1 Course; // association declared in a class
}

// association declared separately
// hello
// katrina added this
association {
	* CourseSection -- * Student registrant;
}
